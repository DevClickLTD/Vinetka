// This page only renders when the user visits the root domain without a locale
// The middleware will handle redirecting to the appropriate localized page
export default function RootPage() {
  return null;
}
